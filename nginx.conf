# server {
#     listen 80;
#     server_name localhost;

#     root /usr/share/nginx/html;
#     index index.html;

#     # Serve the React app
#     location / {
#         try_files $uri /index.html;
#     }

#     # Proxy API calls to FastAPI backend
#     location /api/ {
#         proxy_pass http://app:8000/;  # Service name from docker-compose
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }

#     # Health check
#     location /health {
#         return 200 'OK';
#         add_header Content-Type text/plain;
#     }
# }


# server {
#     listen 80;
#     server_name localhost;   # change to your domain for production

#     # Serve React build
#     root /usr/share/nginx/html;
#     index index.html;

#     # Allow SPA routing
#     location / {
#         try_files $uri /index.html;
#     }

#     # Proxy API requests → FastAPI backend
#     location /api/ {
#         proxy_pass http://app:8000;  # "app" = backend service name in docker-compose
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     # WebSocket support (if future streaming needed)
#     location /ws/ {
#         proxy_pass http://app:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }

#     # Health endpoint for load balancers
#     location /health {
#         return 200 'OK';
#         add_header Content-Type text/plain;
#     }
# }


server {
    listen 8080;

    # PROXY BACKEND API
    location /api/ {
      proxy_pass http://localhost:8000/api/;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

}